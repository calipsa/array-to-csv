{
  "name": "@calipsa/array-to-csv",
  "version": "1.1.0",
  "description": "Convert array to CSV",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "jest --no-cache",
    "lint": "tslint './src/**/*.ts'",
    "build": "rm -rf dist && tsc",
    "prepublishOnly": "npm run build",
    "release": "np"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/calipsa/array-to-csv"
  },
  "keywords": [
    "array",
    "convert",
    "csv"
  ],
  "author": "Anton Veselev",
  "license": "ISC",
  "dependencies": {
    "@types/papaparse": "^5.0.1",
    "papaparse": "^5.1.0"
  },
  "devDependencies": {
    "@types/jest": "^24.0.19",
    "jest": "^24.9.0",
    "np": "^5.1.1",
    "ts-jest": "^24.1.0",
    "tslint": "^5.20.0",
    "typescript": "^3.6.4"
  }
}
